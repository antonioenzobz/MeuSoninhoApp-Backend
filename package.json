{
  "name": "meusoninhoapp-backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "prisma": "bunx prisma generate",
    "build": "bun install && bun run prisma",
    "deploy": "bun run src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "@types/node": "^22.1.0",
    "hono": "^4.5.4",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}